import React from 'react';

const HeadOfFooter = ({title}) => {
    return (
        <div>
            <h3 className='font-bold text-[24px]'>{title}</h3>
        </div>
    );
};

export default HeadOfFooter;